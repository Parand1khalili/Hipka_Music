<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- دسترسی حیاتی برای استریم موزیک و سوکت چت -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- دسترسی‌های موقت برای رفع باگ صدای بلوتوث در گوشی سامسونگ -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".HipkaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Hipka">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hipka">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Person 2 adds the playback Service + MediaSession here (Sprint 2, B1). -->
        <!-- Person 3 adds any chat-related components here if needed (Sprint 2+, C2/C3). -->
    </application>
</manifest>